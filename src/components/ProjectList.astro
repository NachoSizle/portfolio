---
export interface Props {
  projects: any[];
}
import ProjectCard from './ProjectCard.astro';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const { projects } = Astro.props;

const featuredProject = projects.find((p: any) => p.data.featured);
const otherProjects = projects.filter((p: any) => !p.data.featured);
const totalProjects = projects.length;
---

<section class="relative mx-auto max-w-6xl px-6 mb-16 mt-0 bg-transparent" aria-labelledby="projects-section" role="region">

  <!-- Header terminal -->
  <div class="mb-10 relative z-10">
    <span id="projects" tabindex="-1" class="sr-only">{t('projects.title')}</span>
    <div class="flex items-center gap-3 mb-2">
      <span class="font-mono text-xs text-[#3DB882] tracking-widest uppercase select-none">// PROJECTS</span>
      <div class="h-px flex-1 bg-[#1F2630]"></div>
      <span class="font-mono text-xs text-[#3D4B5A] select-none">{totalProjects} entries</span>
    </div>
    <h2
      id="projects-section"
      class="text-3xl sm:text-4xl font-extrabold font-mono tracking-tight text-slate-100 select-none"
    >
      {t('projects.title')} <span style="color: #5EE6A8;">{t('projects.titleHighlight')}</span>
    </h2>
    <p class="mt-2 text-sm text-slate-500 font-mono max-w-2xl select-none" set:html={t('projects.subtitle')} />
  </div>

  <!-- Featured project (si existe) -->
  {featuredProject && (
    <div class="mb-8">
      <div class="flex items-center gap-2 mb-3">
        <span class="px-2 py-0.5 text-xs font-mono font-bold text-[#F5B36B] border border-[#F5B36B]/40 bg-[rgba(245,179,107,0.08)] select-none tracking-widest">
          [FEATURED]
        </span>
        <div class="h-px flex-1 bg-[#1F2630]"></div>
      </div>
      <ProjectCard
        project={featuredProject}
        index={0}
        total={totalProjects}
      />
    </div>
  )}

  <!-- Resto de proyectos -->
  {otherProjects.length > 0 && (
    <div>
      {featuredProject && (
        <div class="flex items-center gap-2 mb-3">
          <span class="font-mono text-xs text-[#3D4B5A] select-none tracking-widest">// OTHER PROJECTS</span>
          <div class="h-px flex-1 bg-[#1F2630]"></div>
        </div>
      )}
      <ul class="space-y-4" role="list">
        {otherProjects.map((project: any, index: number) => (
          <ProjectCard
            project={project}
            index={featuredProject ? index + 1 : index}
            total={totalProjects}
          />
        ))}
      </ul>
    </div>
  )}

  <!-- CTA GitHub -->
  <div class="mt-12 flex justify-center">
    <a
      href="https://github.com/NachoSizle"
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center gap-2 px-5 py-2.5 text-sm font-mono font-bold text-[#5EE6A8] border border-[#5EE6A8]/40 bg-transparent transition-all duration-150 hover:border-[#5EE6A8] hover:-translate-y-0.5 hover:shadow-[2px_2px_0_#3DB882] focus:outline-none focus-visible:ring-1 focus-visible:ring-[#5EE6A8] select-none uppercase"
      aria-label={t('projects.viewMore')}
    >
      {t('projects.likeWhat')} → {t('projects.viewMore')} ↗
    </a>
  </div>
</section>
