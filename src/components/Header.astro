---
import ThemeToggle from "./ThemeToggle.astro";
import LanguagePicker from "./LanguagePicker.astro";
import { getLangFromUrl, useTranslations, getTranslatedPath } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const navLinks = [
  { href: "#about", label: t('nav.about') },
  { href: "#projects", label: t('nav.projects') },
  { href: "#contact", label: t('nav.contact') },
];

// URL de la home en el idioma actual (para fallback de smooth scroll cross-page).
const homeUrl = getTranslatedPath('/', lang);
---

<style>
  #main-nav {
    border-bottom: 1px solid var(--theme-border);
    background: var(--theme-nav-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  #main-nav.scrolled-nav {
    border-bottom-color: var(--theme-border-hover);
    box-shadow: 0 2px 16px 0 var(--theme-nav-shadow);
  }

  @media (prefers-reduced-motion: reduce) {
    #main-nav {
      transition: none;
    }
  }
</style>

<header class="sticky top-0 z-[200] w-full">
  <nav id="main-nav" class="flex gap-3.5 py-3 px-4 sm:px-6 items-center w-full" role="navigation" aria-label="Main navigation">
    <!-- Enlaces de navegación (solo desktop) -->
    <div class="hidden md:flex gap-1 items-center">
      {
        navLinks.map((link) => (
          <a
            href={link.href}
            class="px-3 py-1.5 font-mono text-xs font-bold uppercase text-slate-400 hover:text-[#5EE6A8] transition-colors duration-150 select-none focus:outline-none focus-visible:ring-1 focus-visible:ring-[#5EE6A8]"
          >
            {link.label}
          </a>
        ))
      }
    </div>

    <!-- Spacer -->
    <div class="flex-1"></div>

    <!-- Separador (solo desktop) -->
    <div class="hidden md:block h-4 w-px bg-[#1F2630] mx-1"></div>

    <!-- Controles (siempre visibles) -->
    <div class="flex gap-3 items-center">
      <LanguagePicker id="language-toggle-desktop" />
      <ThemeToggle id="theme-toggle-desktop" />

      <!-- Botón hamburguesa (solo mobile) -->
      <button
        id="mobile-menu-toggle"
        type="button"
        class="md:hidden inline-flex items-center justify-center w-11 h-11 border border-[#1F2630] bg-transparent text-slate-400 hover:text-[#5EE6A8] hover:border-[#5EE6A8]/40 focus:outline-none focus-visible:ring-1 focus-visible:ring-[#5EE6A8] transition-colors duration-150"
        aria-controls="mobile-menu"
        aria-expanded="false"
        aria-label={t('nav.menu.open')}
        data-label-open={t('nav.menu.open')}
        data-label-close={t('nav.menu.close')}
      >
        <svg class="hamburger-open w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg class="hamburger-close w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 6l12 12M18 6L6 18" />
        </svg>
      </button>
    </div>
  </nav>
</header>

<!-- Drawer móvil -->
<div
  id="mobile-menu"
  data-open="false"
  hidden
  class="md:hidden fixed top-0 left-0 right-0 z-150 bg-black/60 backdrop-blur-sm transition-opacity duration-200 ease-out opacity-0 data-[open=true]:opacity-100"
  aria-hidden="true"
>
  <div
    role="dialog"
    aria-modal="true"
    aria-label={t('nav.menu.open')}
    class="absolute top-16 left-0 right-0 border border-[#1F2630] border-t-0 bg-[#0B0D10] p-4 transition-transform duration-200 ease-out -translate-y-2 data-[open=true]:translate-y-0"
    data-mobile-panel
  >
    <ul class="flex flex-col" role="list">
      {navLinks.map((link) => (
        <li>
          <a
            href={link.href}
            class="flex items-center w-full min-h-[48px] px-4 py-3 font-mono text-xs font-bold uppercase text-slate-400 hover:text-[#5EE6A8] hover:bg-[#11151A] focus:outline-none focus-visible:ring-1 focus-visible:ring-[#5EE6A8] transition-colors duration-150"
            data-mobile-link
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
    <!-- Controles de idioma/tema accesibles en mobile -->
    <div class="mt-1 pt-3 border-t border-[#1F2630] flex items-center gap-3 px-4 pb-1">
      <LanguagePicker id="language-toggle-mobile" />
      <ThemeToggle id="theme-toggle-mobile" />
    </div>
  </div>
</div>

<style>
  /* Respeta prefers-reduced-motion: desactiva transiciones del drawer */
  @media (prefers-reduced-motion: reduce) {
    #mobile-menu,
    #mobile-menu [data-mobile-panel] {
      transition: none !important;
    }
  }
</style>

<script is:inline define:vars={{ HOME_URL: homeUrl }}>
  (function () {
    const NAV_ID = 'main-nav';
    const nav = document.getElementById(NAV_ID);

    // Toggle scrolled-nav class based on scroll position
    function updateScrolledNav() {
      if (!nav) return;
      if (window.scrollY > 4) {
        nav.classList.add('scrolled-nav');
      } else {
        nav.classList.remove('scrolled-nav');
      }
    }

    window.addEventListener('scroll', updateScrolledNav, { passive: true });
    // Run once to set initial state
    updateScrolledNav();

    // Helper: compute header height (prefer the header element, fall back to nav)
    function getHeaderHeight() {
      const headerEl = document.querySelector('header');
      if (headerEl) return headerEl.getBoundingClientRect().height;
      return nav ? nav.getBoundingClientRect().height : 0;
    }

    // Resuelve el comportamiento de scroll respetando prefers-reduced-motion.
    // Devuelve 'auto' (salto inmediato sin animación) cuando el usuario lo
    // tiene activado en su sistema; 'smooth' en caso contrario.
    function getScrollBehavior() {
      try {
        if (typeof window !== 'undefined' && typeof window.matchMedia === 'function') {
          const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
          return prefersReducedMotion ? 'auto' : 'smooth';
        }
      } catch (e) {}
      return 'smooth';
    }

    // Smooth scroll to an element accounting for header height and a small gap
    function scrollToElementWithOffset(el, pushState = true) {
      if (!el) return;
      const headerHeight = getHeaderHeight();
      const top = el.getBoundingClientRect().top + window.scrollY - headerHeight;
      // Update URL hash without jumping
      if (pushState && typeof history !== 'undefined') {
        try {
          history.pushState(null, '', `#${el.id}`);
        } catch (e) {}
      }
      window.scrollTo({ top, behavior: getScrollBehavior() });
    }

    // Delegate clicks on in-page anchors inside the nav
    if (nav) {
      nav.addEventListener('click', (ev) => {
        const anchor = ev.target instanceof Element ? ev.target.closest('a[href^="#"]') : null;
        if (!anchor) return;
        const href = anchor.getAttribute('href');
        if (!href || href === '#') return;
        const id = href.slice(1);
        const target = document.getElementById(id);
        if (target) {
          // Section exists on current page, scroll to it
          ev.preventDefault();
          scrollToElementWithOffset(target);
        } else {
          // Section not found, redirect to home page with hash
          ev.preventDefault();
          window.location.href = HOME_URL.replace(/\/$/, '/') + '#' + id;
        }
      });
    }

    // If the page loads with a hash, adjust the initial scroll so the header doesn't cover the title
    document.addEventListener('DOMContentLoaded', () => {
      if (location.hash) {
        const id = location.hash.slice(1);
        const target = document.getElementById(id);
        if (target) {
          // Timeout to allow the browser to finish any native jump before we correct it
          setTimeout(() => scrollToElementWithOffset(target, false), 50);
        }
      }
    });

    // ──────────────────────────────────────────────────────────────────
    // Mobile menu (GYM-98): hamburger toggle, Escape, click outside,
    // focus management y respeto de prefers-reduced-motion (vía CSS).
    // ──────────────────────────────────────────────────────────────────
    const toggle = document.getElementById('mobile-menu-toggle');
    const panel = document.getElementById('mobile-menu');
    const iconOpen = toggle ? toggle.querySelector('.hamburger-open') : null;
    const iconClose = toggle ? toggle.querySelector('.hamburger-close') : null;
    let lastFocused = null;

    function setMobileMenu(open) {
      if (!toggle || !panel) return;
      toggle.setAttribute('aria-expanded', open ? 'true' : 'false');
      const labelOpen = toggle.getAttribute('data-label-open') || 'Open menu';
      const labelClose = toggle.getAttribute('data-label-close') || 'Close menu';
      toggle.setAttribute('aria-label', open ? labelClose : labelOpen);
      panel.setAttribute('data-open', open ? 'true' : 'false');
      panel.setAttribute('aria-hidden', open ? 'false' : 'true');
      if (open) {
        lastFocused = document.activeElement;
        panel.hidden = false;
        document.body.style.overflow = 'hidden';
        if (iconOpen) iconOpen.classList.add('hidden');
        if (iconClose) iconClose.classList.remove('hidden');
        // Focus first link inside the panel
        const firstLink = panel.querySelector('[data-mobile-link]');
        if (firstLink instanceof HTMLElement) firstLink.focus();
      } else {
        document.body.style.overflow = '';
        if (iconOpen) iconOpen.classList.remove('hidden');
        if (iconClose) iconClose.classList.add('hidden');
        // Hide after transition
        setTimeout(() => { if (panel.getAttribute('data-open') === 'false') panel.hidden = true; }, 200);
        if (lastFocused instanceof HTMLElement) lastFocused.focus();
      }
    }

    if (toggle && panel) {
      toggle.addEventListener('click', () => {
        const open = toggle.getAttribute('aria-expanded') !== 'true';
        setMobileMenu(open);
      });

      // Cierre al pulsar Escape
      document.addEventListener('keydown', (ev) => {
        if (ev.key === 'Escape' && toggle.getAttribute('aria-expanded') === 'true') {
          setMobileMenu(false);
        }
      });

      // Cierre al hacer clic fuera del panel (sobre el backdrop)
      panel.addEventListener('click', (ev) => {
        const inPanel = ev.target instanceof Element ? ev.target.closest('[data-mobile-panel]') : null;
        if (!inPanel) setMobileMenu(false);
      });

      // Cierre al navegar a un enlace del panel
      panel.querySelectorAll('[data-mobile-link]').forEach((a) => {
        a.addEventListener('click', () => setMobileMenu(false));
      });

      // Focus trap básico (Tab/Shift+Tab dentro del panel)
      panel.addEventListener('keydown', (ev) => {
        if (ev.key !== 'Tab') return;
        const focusables = panel.querySelectorAll('a, button, [tabindex]:not([tabindex="-1"])');
        if (!focusables.length) return;
        const first = focusables[0];
        const last = focusables[focusables.length - 1];
        const active = document.activeElement;
        if (ev.shiftKey && active === first) { ev.preventDefault(); last.focus(); }
        else if (!ev.shiftKey && active === last) { ev.preventDefault(); first.focus(); }
      });
    }
  })();
</script>
