---
import SocialButton from '../islands/SocialButton.tsx';

interface Props {
  href: string;
  label: string;
  iconName: "github" | "linkedin" | "threads";
  iconClassName?: string;
}

const { href, label, iconName, iconClassName } = Astro.props;
---

<SocialButton 
  href={href} 
  label={label} 
  iconName={iconName} 
  iconClassName={iconClassName}
  client:visible 
/>
